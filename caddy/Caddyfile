:80 {
    log {
        output stdout
    }

    # Use basic_auth (with an underscore) instead of basicauth
    basic_auth / {
        {$CADDY_BASIC_AUTH_USER} {$CADDY_BASIC_AUTH_HASH}
    }

    reverse_proxy grafana:3000
}
